import React from 'react'
import { View, StyleSheet, Text } from 'react-native';

const ISSItemScreen = (props) => {

  return (
    <View>
        <Text>{item.url}</Text>
    </View>
  )
}

export default ISSItemScreen;